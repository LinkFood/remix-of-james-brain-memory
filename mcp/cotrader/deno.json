{
  "tasks": {
    "start": "deno run --allow-all server.ts",
    "smoke": "deno run --allow-all smoke-test.ts",
    "verify": "deno run --allow-all tests/run_verification.ts"
  },
  "lock": false,
  "fmt": {
    "lineWidth": 100,
    "singleQuote": true,
    "semiColons": true
  }
}
