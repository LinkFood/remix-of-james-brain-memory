# check_supabase_table_refs.sh allowlist — table names that look like orphans
# but are NOT actually broken references. One name per line. Comments start
# with #. Each entry MUST have a comment above it explaining WHY it's exempt.
# Adding new entries requires James approval.
#
# Removing entries requires verifying the underlying issue is resolved.
#
# COMMENT POLICY (added 2026-05-05 per messages Phase A audit editorial note):
# Allowlist comments must be EVIDENCE-BASED or HONEST-UNKNOWN. No
# speculative hypotheses.
#
#   GOOD — evidence-based:
#     # `dumps` — verified false-positive: 3 references are
#     # supabase.storage.from('dumps') — Storage bucket calls, not table.
#
#   GOOD — honest-unknown:
#     # `messages` — unknown — pending diagnose-only Phase A audit.
#
#   BAD — speculative hypothesis (premature framing misleads later audits):
#     # `messages` — Possibly RLS-filtered or schema-cache stale.
#
# The 2026-05-05 messages audit found that "Possibly RLS-filtered" was a
# confident misdiagnosis — actual cause was a deliberate 2026-01-23 table
# drop. The hypothesis-as-comment misframed audit thinking 3 days later.
# Don't repeat — write what you know, mark what you don't.

# [removed 2026-05-05 dumps Phase B] `dumps` was a Supabase Storage bucket,
# not a Postgres table. Phase A audit confirmed all 3 references are
# `supabase.storage.from('dumps')`. The script's awk extractor was tightened
# to filter `.storage.from(...)` references — `dumps` is no longer detected
# as a candidate at all, so the allowlist entry was removed (not just kept
# perma-suppressed). See `docs/audit/2026-05-05-dumps-orphan-phase-a.md`.

# [removed 2026-05-05 messages Phase B] `messages` was deliberately DROPPED
# 2026-01-23 in the multi-provider-chat → brain-dump architecture pivot. The
# 2 surviving references in calculate-importance/index.ts (SELECT line 75 +
# UPDATE line 135) were both gated by `if (messageId)` — an unreached
# branch in 103 days (smart-save always passes inline content; importance
# scores write to entries.importance_score directly). Phase B B1 deleted
# the dead branches; the orphan no longer exists. Allowlist comment that
# previously read "Possibly RLS-filtered or schema-cache stale" was a
# confident misdiagnosis (see header policy added in PR #20 amendment).
# Audit: docs/audit/2026-05-05-messages-orphan-phase-a.md.
